import { OrderstatusNamePipe } from './orderstatus-name.pipe';

describe('OrderstatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderstatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
