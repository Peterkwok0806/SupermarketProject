import { BackendImagePipe } from './backend-image.pipe';

describe('BackendImagePipe', () => {
  it('create an instance', () => {
    const pipe = new BackendImagePipe();
    expect(pipe).toBeTruthy();
  });
});
